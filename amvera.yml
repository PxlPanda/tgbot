meta:
  environment: python
  toolchain:
    name: pip
    version: "3.8"
build:
  requirementsPath: tgbot/requirements.txt
run:
  persistenceMount: /data
  containerPort: "80"
  scriptName: tgbot/quest_bot.py
