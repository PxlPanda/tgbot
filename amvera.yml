meta:
  environment: python
  toolchain:
    name: pip
    version: "3.12"
build:
  requirementsPath: requirements.txt
run:
  workdir: /tgbot
  persistenceMount: /tgbot/config.json
  containerPort: "80"
  command: python /tgbot/quest_bot.py
