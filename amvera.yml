meta:
  environment: python
  toolchain:
    name: pip
    version: "3.12"

build:
  requirementsPath: requirements.txt
  additionalFiles:
    - quest_bot.py
    - config.json

run:
  workdir: /app
  scriptName: quest_bot.py
  containerPort: 80
