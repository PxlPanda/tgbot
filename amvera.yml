meta:
  environment: python
  toolchain:
    name: pip
    version: "3.12"

build:
  requirementsPath: requirements.txt

run:
  scriptName: quest_bot.py
  command: python quest_bot.py
  persistenceMount: /tgbot/config.json
  containerPort: 80
