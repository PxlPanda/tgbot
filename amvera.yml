meta:
  environment: python
  toolchain:
    name: pip
    version: "3.12"
build:
  requirementsPath: requirements.txt
run:
  persistenceMount: /app/config.json
  containerPort: 80
  command: python /app/quest_bot.py
