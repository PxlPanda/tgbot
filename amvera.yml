meta:
  environment: python
  toolchain:
    name: pip
    version: "3.12"

build:
  requirementsPath: requirements.txt
run:
  command: mkdir -p /app && cp -f quest_bot.py config.json /app/ && cd /app && ls -la && python quest_bot.py
  containerPort: 80
